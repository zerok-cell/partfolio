import { Funnel_Sans, Ledger } from "next/font/google";

export const ledger = Ledger({
 subsets: ["latin"],
 weight: "400"
});

export const funnel_sans = Funnel_Sans({
 subsets: ["latin"],
 weight: ["500", "700"]
});